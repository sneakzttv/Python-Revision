# tuples uses to store a sequence of objects, immutable can't be changed

# tuple uses brackets
numbers = (1, 2, 3, 3)
# returns tuple values
print(numbers)
# returns occurences of values
print(numbers.count(2))
# returns first index of value
print(numbers.index(2))